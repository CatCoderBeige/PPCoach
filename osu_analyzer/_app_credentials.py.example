"""Vorlage fuer deine eigenen osu! App-Zugangsdaten.

1. Kopiere diese Datei zu `_app_credentials.py` (gleicher Ordner).
2. Trage deine eigene Client-ID/Secret ein (registrieren unter
   https://osu.ppy.sh/home/account/edit#new-oauth-application).
3. `_app_credentials.py` wird von PyInstaller automatisch mit in die .exe gepackt
   und ist NICHT eingecheckt (siehe .gitignore) - Kunden sehen/brauchen sie nie.
"""

CLIENT_ID = "DEINE_OSU_CLIENT_ID"
CLIENT_SECRET = "DEIN_OSU_CLIENT_SECRET"
